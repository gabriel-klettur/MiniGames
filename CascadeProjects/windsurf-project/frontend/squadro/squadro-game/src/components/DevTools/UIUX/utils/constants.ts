// Shared constants used across UIUX panels

export const MIN_ORIGIN = -100;
export const MAX_ORIGIN = 100;

export const MIN_SCALE = 0.8;
export const MAX_SCALE = 1.2;

export const MIN_ANIM_MS = 0;
export const MAX_ANIM_MS = 2000;

export const MIN_HEIGHT = 24;
export const MAX_HEIGHT = 120;

export const MIN_WS = 0.5; // width scale
export const MAX_WS = 2.0;

export const DEFAULT_PIECE_SCALE = 0.7;
export const DEFAULT_HEIGHT = 44;
export const DEFAULT_WIDTH_SCALE = 1;
export const DEFAULT_ANIM_MS = 200;

// Rotation animation duration (at direction change)
export const MIN_ROTATE_MS = 0;
export const MAX_ROTATE_MS = 3000;
export const DEFAULT_ROTATE_MS = 500;

// Board scale (visual size of the grid relative to available space)
export const MIN_BOARD_SCALE = 0.5;
export const MAX_BOARD_SCALE = 2.0;
export const DEFAULT_BOARD_SCALE = 1.0;
