// Depth bounds
export const DEPTH_MIN = 1;
export const DEPTH_MAX = 10;

// Time bounds (seconds)
export const TIME_MIN_SECONDS = 0;
export const TIME_MAX_SECONDS = 30;

// Default time for manual mode (seconds)
// As per project preference: default AI thinking time should be 8 seconds (manual mode)
export const DEFAULT_TIME_SECONDS = 8;

// UI defaults
export const ELAPSED_INTERVAL_MS_DEFAULT = 100; // interval for elapsed timer UI
export const TOP_MOVES_LIMIT_DEFAULT = 6; // number of root moves to display

// Evaluation normalization constant (for tanh scaling)
export const EVAL_TANH_DIVISOR = 25;

