import React from 'react';

const Controls: React.FC = () => (
  <div className="controls">
    <div className="kpi kpi--muted">Controles avanzados (placeholder Soluna).</div>
  </div>
);

export default Controls;
